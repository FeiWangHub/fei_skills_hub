# Knowledge

This directory is for durable notes that are not tied to a specific memory schema.

Suggested structure:

- `howto/` for step-by-step guides
- `snippets/` for reusable code blocks
- `references/` for links and curated docs

Example: capture recurring command patterns, internal conventions, and troubleshooting notes here.
